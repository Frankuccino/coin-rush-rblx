-- Centralized round configuration.
-- Change values here; do not edit hardcoded literals in services.

return {

	-- Lobby
	MinPlayers = 1,

	-- Countdown before round starts (seconds)
	CountdownTime = 5,

	-- Active round duration (seconds)
	RoundTime = 10,

	-- Intermission after round ends (seconds)
	IntermissionTime = 5,
}
